#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
    vector<int> vec = {1, 2, 3, 4, 5};
    rotate(vec.rbegin(), vec.rbegin() + 2, vec.rend());
    for(int x : vec) cout << x << " ";
    return 0;
}

Result-4 5 1 2 3
